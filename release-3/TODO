-*- outline -*-

* Make an automatic script to update the template files (./pot)

The directory ./pot was once a directory with only POT files.  But
now, it contains all the files that indicate which files go into the l10n dir.
When updating POT files, one needs to check that those other files are not
destroyed.



